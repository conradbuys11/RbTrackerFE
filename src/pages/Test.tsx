const Test = () => {
  return (
    <></>

    // <div>
    //   <h1>List of teams in DB:</h1>
    //   {teams.map((team) => (
    //     <div key={team.id}>
    //       <p>
    //         {team.location} {team.name} ({team.code}). {team.conference}{" "}
    //         {team.division}.
    //       </p>
    //     </div>
    //   ))}
    // </div>
  );
};

export default Test;
